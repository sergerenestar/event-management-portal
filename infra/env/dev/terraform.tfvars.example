# Copy this file to terraform.tfvars and fill in the values.
# NEVER commit terraform.tfvars — it is in .gitignore.

location           = "eastus"
sql_admin_login    = "sqladmin"
sql_admin_password = "JesusIsLord!"
